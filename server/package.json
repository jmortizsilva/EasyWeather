{
  "name": "easyweather-alerts-server",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor de avisos de temperatura de EasyWeather (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
